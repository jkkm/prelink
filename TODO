- undo mode
- make sure there are no memory leaks
- take a list of library directories from config file, prelink all libs
  in those directories at once, then all binaries in given paths
  (keep the prelink one library or one binary mode under some option)
- more ports
- more testing
